# راهنمای رفع مشکلات نصب کتابخانه‌ها

## ✅ مشکل حل شد!

کتابخانه `python-telegram-bot` با موفقیت نصب شده است.

## 🔧 راه‌حل‌های رایج مشکلات نصب

### 1. مشکل: دستور `python` کار نمی‌کند
**راه‌حل:** از `py` به جای `python` استفاده کنید
```bash
# به جای این:
python -m pip install python-telegram-bot

# از این استفاده کنید:
py -m pip install python-telegram-bot
```

### 2. مشکل: خطای شبکه یا فیلترینگ
**راه‌حل‌ها:**
```bash
# استفاده از mirror چینی
py -m pip install python-telegram-bot -i https://pypi.tuna.tsinghua.edu.cn/simple/

# استفاده از trusted hosts
py -m pip install python-telegram-bot --trusted-host pypi.org --trusted-host pypi.python.org

# استفاده از VPN
```

### 3. مشکل: خطای مجوز (Permission Error)
**راه‌حل:**
```bash
# اجرای PowerShell به عنوان Administrator
# یا استفاده از --user flag
py -m pip install python-telegram-bot --user
```

### 4. مشکل: نسخه Python قدیمی
**راه‌حل:**
- Python 3.8 یا بالاتر نصب کنید
- از `py --version` برای بررسی نسخه استفاده کنید

### 5. مشکل: pip نصب نشده
**راه‌حل:**
```bash
# نصب pip
py -m ensurepip --upgrade

# یا دانلود get-pip.py
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
py get-pip.py
```

## 🧪 تست نصب

برای اطمینان از نصب صحیح:
```bash
py test_installation.py
```

## 📞 درخواست کمک

اگر همچنان مشکل دارید:
1. خطای دقیق را کپی کنید
2. نسخه Python را بررسی کنید: `py --version`
3. نسخه pip را بررسی کنید: `py -m pip --version`
4. با تیم پشتیبانی تماس بگیرید

## 🎉 موفقیت!

اگر پیام "🎉 همه چیز درست کار می‌کند!" را می‌بینید، نصب موفق بوده و می‌توانید ربات را اجرا کنید:

```bash
py run_bot.py
``` 