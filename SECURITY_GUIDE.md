# 🔒 راهنمای امنیت ربات تلگرام

## ⚠️ نکات مهم امنیتی

### ۱. محافظت از توکن ربات
- ❌ **هرگز** توکن ربات را در کد قرار ندهید
- ✅ از متغیرهای محیطی استفاده کنید
- ✅ فایل `.env` را در `.gitignore` قرار دهید

### ۲. فایل‌های حساس
فایل‌های زیر هرگز نباید در GitHub آپلود شوند:
- `.env` (متغیرهای محیطی)
- `config_secret.py` (تنظیمات محرمانه)
- `secrets.py` (کلیدهای رمزنگاری)
- فایل‌های حاوی شماره تلفن، ایمیل، آدرس

### ۳. تنظیمات امن برای Railway

#### متغیرهای محیطی Railway:
```
BOT_TOKEN=8468544112:AAH9P9J6uBTuhrveLnT17yblVnP7_Ebxgaw
ADMIN_USER_ID=YOUR_TELEGRAM_USER_ID
```

#### نحوه تنظیم:
1. در Railway Dashboard، روی پروژه کلیک کنید
2. به تب "Variables" بروید
3. متغیرهای بالا را اضافه کنید

## 🔧 تنظیمات محلی امن

### ۱. ایجاد فایل `.env`:
```bash
# در پوشه پروژه، فایل .env ایجاد کنید
BOT_TOKEN=8468544112:AAH9P9J6uBTuhrveLnT17yblVnP7_Ebxgaw
ADMIN_USER_ID=YOUR_TELEGRAM_USER_ID
```

### ۲. نصب python-dotenv:
```bash
py -m pip install python-dotenv
```

### ۳. به‌روزرسانی `requirements.txt`:
```
python-telegram-bot==21.7
python-dotenv==1.0.0
```

## 🛡️ اقدامات امنیتی اضافی

### ۱. محدود کردن دسترسی ربات:
- فقط کاربران مجاز بتوانند از ربات استفاده کنند
- محدودیت تعداد درخواست‌ها
- لاگ کردن فعالیت‌های مشکوک

### ۲. رمزنگاری داده‌ها:
- رمزنگاری اطلاعات دانش‌آموزان
- استفاده از HTTPS
- پشتیبان‌گیری امن

### ۳. به‌روزرسانی منظم:
- به‌روزرسانی کتابخانه‌ها
- بررسی آسیب‌پذیری‌ها
- پچ کردن امنیتی

## 🚨 در صورت نشت اطلاعات

### ۱. فوری:
- توکن ربات را در @BotFather تغییر دهید
- متغیرهای محیطی را به‌روزرسانی کنید
- کد را از GitHub حذف کنید

### ۲. بررسی:
- لاگ‌ها را بررسی کنید
- فعالیت‌های مشکوک را شناسایی کنید
- امنیت را تقویت کنید

## ✅ چک‌لیست امنیت

قبل از آپلود به GitHub:
- [ ] توکن ربات در کد نیست
- [ ] فایل `.env` در `.gitignore` است
- [ ] اطلاعات شخصی حذف شده‌اند
- [ ] فایل‌های حساس نادیده گرفته می‌شوند
- [ ] متغیرهای محیطی تنظیم شده‌اند

## 📞 پشتیبانی امنیتی

در صورت بروز مشکل امنیتی:
- 📧 ایمیل: HamrahBaOstad@gmail.com
- 💬 تلگرام: @Ostad_Hatami

---

**امنیت اولویت اول است! 🔒** 