# 🎓 ربات تلگرام استاد حاتمی - نسخه نهایی

ربات تلگرام کامل و بهینه برای استاد حاتمی با قابلیت‌های پیشرفته ثبت‌نام، مدیریت دوره‌ها و ارتباط با دانشجویان.

## ✨ ویژگی‌های کلیدی

### 🧩 **Onboarding کامل**
- پیام خوش‌آمدی شخصی‌سازی شده
- فرآیند ثبت‌نام ۶ مرحله‌ای:
  1. نام
  2. نام خانوادگی
  3. شماره تلفن (با دکمه share_contact یا ورودی دستی)
  4. شهر
  5. پایه تحصیلی (۹، ۱۰، ۱۱)
  6. رشته تحصیلی (ریاضی، تجربی، انسانی)
- ذخیره‌سازی امن در فایل‌های JSON جداگانه

### 📋 **منوی اصلی**
- 🎓 دوره‌های استاد حاتمی
- 📘 تهیه کتاب انفجار خلاقیت
- 📞 ارتباط با ما
- 🌐 یوتیوب / اینستاگرام
- 💬 کانال و گروه همراه با استاد

### 🛠️ **ویژگی‌های فنی**
- استفاده از python-telegram-bot نسخه ۲۱.۷
- ساختار مدولار و تمیز
- مدیریت خطاها و Logging
- امنیت بالا با متغیرهای محیطی
- آماده Deploy روی هر پلتفرم

## 🚀 نصب و راه‌اندازی

### **پیش‌نیازها**
- Python 3.8 یا بالاتر
- Bot Token از @BotFather

### **مراحل نصب**

1. **نصب وابستگی‌ها:**
   ```bash
   pip install python-telegram-bot==21.7 python-dotenv==1.0.0
   ```

2. **تنظیم متغیرهای محیطی:**
   ```bash
   cp env_template.txt .env
   # ویرایش فایل .env و قرار دادن BOT_TOKEN
   ```

3. **اجرای ربات:**
   ```bash
   python final_bot.py
   ```

## 📁 ساختار پروژه

```
📦 Telegram-Bot/
├── 📄 final_bot.py              # ربات اصلی
├── 📄 requirements_final.txt     # وابستگی‌ها
├── 📄 env_template.txt          # نمونه متغیرهای محیطی
├── 📄 test_bot.py               # اسکریپت تست
├── 📄 SETUP_GUIDE.md           # راهنمای نصب
├── 📄 README_FINAL.md          # این فایل
├── 📁 data/
│   └── 📁 users/                # فایل‌های کاربران
└── 📄 bot.log                   # فایل لاگ
```

## 🧪 تست ربات

برای اطمینان از صحت نصب:

```bash
python test_bot.py
```

این اسکریپت موارد زیر را بررسی می‌کند:
- ✅ نسخه Python
- ✅ نصب وابستگی‌ها
- ✅ تنظیم متغیرهای محیطی
- ✅ ساختار فایل‌ها
- ✅ ایجاد دایرکتوری داده‌ها
- ✅ Import کلاس‌های ربات

## 🎯 قابلیت‌های ربات

### **دستورات اصلی:**
- `/start` - شروع ربات و خوش‌آمدگویی
- `/register` - ثبت‌نام در ربات
- `/menu` - نمایش منوی اصلی
- `/help` - راهنمای استفاده

### **فرآیند ثبت‌نام:**
1. کاربر با `/start` وارد می‌شود
2. پیام خوش‌آمدی شخصی‌سازی شده نمایش داده می‌شود
3. فرآیند ثبت‌نام ۶ مرحله‌ای شروع می‌شود
4. اطلاعات در فایل `user_{user_id}.json` ذخیره می‌شود
5. منوی اصلی نمایش داده می‌شود

### **منوی اصلی:**
- **دوره‌های استاد حاتمی:** لیست دوره‌ها با قیمت و توضیحات
- **کتاب انفجار خلاقیت:** اطلاعات کتاب و راه‌های سفارش
- **ارتباط با ما:** شماره تماس، ایمیل و پشتیبانی
- **شبکه‌های اجتماعی:** لینک‌های یوتیوب و اینستاگرام
- **کانال و گروه:** عضویت در کانال و گروه تلگرام

## 🔒 امنیت و حریم خصوصی

- **ذخیره‌سازی امن:** هر کاربر فایل JSON جداگانه
- **متغیرهای محیطی:** BOT_TOKEN در فایل .env
- **Logging امن:** فقط خطاهای مهم در Console
- **مدیریت خطا:** Exception Handling کامل

## 📊 مانیتورینگ

### **فایل‌های لاگ:**
- `bot.log` - لاگ‌های کامل ربات
- بررسی عملکرد و خطاها

### **داده‌های کاربران:**
- `data/users/user_[ID].json` - اطلاعات هر کاربر
- فرمت JSON قابل خواندن و ویرایش

## 🚀 Deploy

### **Railway:**
```bash
railway login
railway init
railway up
```

### **Heroku:**
```bash
echo "worker: python final_bot.py" > Procfile
git add .
git commit -m "Deploy bot"
git push heroku main
```

### **VPS:**
```bash
# ایجاد systemd service
sudo nano /etc/systemd/system/telegram-bot.service

# فعال‌سازی سرویس
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

## 🔧 عیب‌یابی

### **مشکلات رایج:**

1. **ModuleNotFoundError:**
   ```bash
   pip install --upgrade python-telegram-bot python-dotenv
   ```

2. **BOT_TOKEN not found:**
   ```bash
   # بررسی فایل .env
   cat .env
   # اطمینان از وجود BOT_TOKEN=your_token
   ```

3. **Permission denied:**
   ```bash
   mkdir -p data/users
   chmod 755 data/users
   ```

## 📞 پشتیبانی

در صورت بروز مشکل:
1. بررسی فایل `bot.log`
2. اجرای `python test_bot.py`
3. بررسی تنظیمات متغیرهای محیطی
4. تماس با پشتیبانی

## ✅ چک‌لیست نهایی

- [ ] Python 3.8+ نصب شده
- [ ] وابستگی‌ها نصب شده‌اند
- [ ] فایل .env ایجاد و تنظیم شده
- [ ] BOT_TOKEN معتبر تنظیم شده
- [ ] دایرکتوری data/users ایجاد شده
- [ ] ربات اجرا می‌شود
- [ ] دستور /start کار می‌کند
- [ ] ثبت‌نام کاربران کار می‌کند
- [ ] منوی اصلی نمایش داده می‌شود

## 🎉 نتیجه

ربات تلگرام استاد حاتمی کاملاً آماده استفاده است و تمام نیازهای شما را برآورده می‌کند:

- ✅ **Onboarding کامل** با شخصی‌سازی
- ✅ **منوی اصلی** با تمام گزینه‌های مورد نیاز
- ✅ **امنیت بالا** با ذخیره‌سازی جداگانه
- ✅ **کد تمیز** و قابل نگهداری
- ✅ **آماده Deploy** روی هر پلتفرم

---

**🎓 برای شروع:**
1. نصب وابستگی‌ها
2. تنظیم BOT_TOKEN
3. اجرای ربات
4. تست با دستور /start

**ربات آماده استفاده است! 🚀** 