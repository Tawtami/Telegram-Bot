# راهنمای سریع ادمین‌ها 📚

## دسترسی و اعلان‌ها

- شناسه ادمین‌ها در `ADMIN_USER_IDS` تنظیم می‌شود.
- اعلان‌ها: ثبت‌نام جدید، خریدهای Pending، تایید/رد پرداخت، و به‌روزرسانی فهرست شرکت‌کنندگان.

## دستورات مدیریتی

- **سفارش‌ها**
  - `/orders [pending|approved|rejected]`
  - `/orders_ui` (رابط صفحه‌بندی با دکمه‌های تایید/رد)
- **کاربران**
  - `/user_search <query>`
  - `/profile_history <telegram_id>`
- **فهرست‌ها**
  - `/list_books`
  - `/list_free <grade>`
  - `/list_special <slug>`
  - برای لیست‌های بزرگ، فایل CSV ارسال می‌شود.
- **پیام‌رسانی**
  - `/broadcast <message>`
  - `/broadcast_grade <grade> <message>`
- **وضعیت و آمار**
  - `/status`
  - `/metrics [csv]`

## تایید/رد پرداخت

1. رسید دریافت → سفارش Pending در DB → ارسال به ادمین‌ها.
2. دکمه‌های «✅ تایید» یا «❌ رد» را بزنید (اتمیک؛ فقط اولین تصمیم اعمال می‌شود).
3. دکمه‌ها برای هر دو ادمین غیرفعال می‌شود؛ به کاربر اطلاع داده می‌شود.
4. در تایید دوره، فهرست شرکت‌کنندگان به‌روزرسانی و ارسال می‌شود.

## امنیت

- `ENCRYPTION_KEY` در تولید الزامی است.
- هیچ PII در لاگ‌ها ذخیره نمی‌شود؛ توکن‌ها رداکت می‌شوند.
- وب‌هوک با Secret Token و HTTPS.

## راه‌اندازی سریع

1. تنظیم متغیرها: `BOT_TOKEN`, `ADMIN_USER_IDS`, `ENCRYPTION_KEY`, `WEBHOOK_URL`, `DATABASE_URL`.
2. استقرار Railway (وب‌هوک فعال).
3. بررسی `/status` و `/metrics`.

## داشبورد وب (اختیاری)

- مسیر: `/admin?token=<ADMIN_DASHBOARD_TOKEN>&status=pending`
- اقدامات: `/admin/act?token=...&id=<purchase_id>&action=approve|reject`
- `ADMIN_DASHBOARD_TOKEN` را در محیط تنظیم کنید.
