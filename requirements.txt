# Core dependencies
# Pin to PTB v20.x and include webhooks extra for Application.run_webhook support
python-telegram-bot[webhooks]>=20.3,<21.0
python-dotenv>=1.0.0

# Utility packages
aiohttp>=3.8.0
python-json-logger>=2.0.0
pytz  # For timezone handling