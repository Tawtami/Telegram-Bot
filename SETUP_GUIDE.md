# 🚀 راهنمای راه‌اندازی ربات استاد حاتمی

Setup Guide for Ostad Hatami Bot

## 📋 مرحله ۱: دریافت توکن ربات

1. به [@BotFather](https://t.me/botfather) در تلگرام بروید
2. دستور `/newbot` را ارسال کنید
3. نام ربات را وارد کنید: **Ostad Hatami Bot**
4. نام کاربری ربات را وارد کنید: **OstadHatami_bot**
5. توکن ربات را کپی کنید (مثل: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## ⚙️ مرحله ۲: تنظیم فایل config.py

1. فایل `config.py` را باز کنید
2. خط زیر را پیدا کنید:
   ```python
   BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
   ```
3. توکن کپی شده را جایگزین کنید:
   ```python
   BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
   ```

## 📞 مرحله ۳: بروزرسانی اطلاعات تماس

در فایل `config.py`، اطلاعات زیر را با اطلاعات واقعی استاد حاتمی بروزرسانی کنید:

### اطلاعات تماس:
```python
CONTACT_INFO = {
    "whatsapp": "+98 912 123 4567",  # شماره واتساپ واقعی
    "phone": "+98 21 1234 5678",     # شماره تلفن واقعی
    "telegram": "@OstadHatami",      # نام کاربری تلگرام واقعی
    "email": "ostad.hatami@example.com",  # ایمیل واقعی
    "address": "تهران، خیابان ولیعصر",    # آدرس واقعی
    "working_hours": "شنبه تا چهارشنبه: ۹ صبح تا ۶ عصر\nپنجشنبه: ۹ صبح تا ۱ ظهر"
}
```

### شبکه‌های اجتماعی:
```python
SOCIAL_LINKS = {
    "instagram": "@ostad_hatami_math",      # اینستاگرام واقعی
    "youtube": "Ostad Hatami Math Channel", # یوتیوب واقعی
    "telegram_channel": "@OstadHatamiChannel", # کانال تلگرام واقعی
    "website": "www.ostadhatami.ir"        # وب‌سایت واقعی
}
```

## 📚 مرحله ۴: تنظیم دوره‌های آموزشی

در فایل `config.py`، دوره‌های آموزشی را بروزرسانی کنید:

```python
COURSES = {
    "دهم": {
        "جبر و معادله": {"price": 250000, "duration": "12 جلسه", "type": "آنلاین"},
        "هندسه تحلیلی": {"price": 250000, "duration": "10 جلسه", "type": "آنلاین"}
    },
    "یازدهم": {
        "حسابان": {"price": 300000, "duration": "15 جلسه", "type": "آنلاین"},
        "آمار و احتمال": {"price": 300000, "duration": "12 جلسه", "type": "آنلاین"}
    },
    "دوازدهم": {
        "مشتق و حد": {"price": 350000, "duration": "18 جلسه", "type": "آنلاین"},
        "انتگرال": {"price": 350000, "duration": "16 جلسه", "type": "آنلاین"},
        "هندسه": {"price": 350000, "duration": "14 جلسه", "type": "آنلاین"}
    }
}
```

## 🛠️ مرحله ۵: نصب وابستگی‌ها

در ترمینال، دستور زیر را اجرا کنید:

```bash
cd dad
pip install -r requirements.txt
```

## 🚀 مرحله ۶: اجرای ربات

### برای تست محلی:
```bash
python run_bot.py
```

### یا مستقیماً:
```bash
python mathbot.py
```

## ✅ مرحله ۷: تست ربات

1. در تلگرام، ربات `@OstadHatami_bot` را پیدا کنید
2. دستور `/start` را ارسال کنید
3. فرآیند ثبت‌نام را تست کنید
4. منوهای مختلف را بررسی کنید

## 🔧 مرحله ۸: مدیریت دانش‌آموزان

برای مدیریت دانش‌آموزان ثبت‌نام شده:

```bash
python admin.py
```

این ابزار امکانات زیر را فراهم می‌کند:
- نمایش همه دانش‌آموزان
- جستجوی دانش‌آموز خاص
- آمار ثبت‌نام
- Export به فایل CSV
- حذف دانش‌آموز

## 🌐 مرحله ۹: راه‌اندازی روی سرور (اختیاری)

برای راه‌اندازی ۲۴ ساعته روی سرور:

### Railway:
1. حساب کاربری در [Railway](https://railway.app) ایجاد کنید
2. پروژه را به GitHub متصل کنید
3. متغیر محیطی `BOT_TOKEN` را تنظیم کنید
4. Deploy کنید

### Render:
1. حساب کاربری در [Render](https://render.com) ایجاد کنید
2. Web Service جدید ایجاد کنید
3. Repository را متصل کنید
4. Build Command: `pip install -r requirements.txt`
5. Start Command: `python mathbot.py`

## 📱 دستورات ربات

- `/start` - شروع ربات و ثبت‌نام
- `/cancel` - لغو فرآیند ثبت‌نام

## 🆘 عیب‌یابی

### مشکل: "توکن ربات تنظیم نشده است"
**راه حل:** توکن را در فایل `config.py` وارد کنید

### مشکل: "python-telegram-bot نصب نشده است"
**راه حل:** `pip install python-telegram-bot` را اجرا کنید

### مشکل: ربات پاسخ نمی‌دهد
**راه حل:** 
1. توکن را بررسی کنید
2. اتصال اینترنت را چک کنید
3. ربات را مجدداً راه‌اندازی کنید

## 📞 پشتیبانی

در صورت بروز مشکل:
1. فایل‌های log را بررسی کنید
2. تنظیمات را دوباره چک کنید
3. ربات را مجدداً راه‌اندازی کنید

---

**🎉 تبریک! ربات استاد حاتمی آماده استفاده است!** 